/**
 * Test for Refresher.
 * Runs with mocha.
 */
'use strict'

const Refresher = require('../lib/Refresher')
const { ok, equal } = require('assert')

describe('refresher', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
