/**
 * Test for Resource.
 * Runs with mocha.
 */
'use strict'

const Resource = require('../lib/Resource')
const { ok, equal } = require('assert')

describe('resource', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
