/**
 * Test for exportImportMix.
 * Runs with mocha.
 */
'use strict'

const exportImportMix = require('../lib/mixins/exportImportMix')
const { ok, equal } = require('assert')

describe('export-import-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
