/**
 * Test for LogResource.
 * Runs with mocha.
 */
'use strict'

const LogResource = require('../lib/resources/LogResource')
const { ok, equal } = require('assert')

describe('log-resource', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
