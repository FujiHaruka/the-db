/**
 * Test for SchemaResource.
 * Runs with mocha.
 */
'use strict'

const SchemaResource = require('../lib/resources/SchemaResource')
const { ok, equal } = require('assert')

describe('schema-resource', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
